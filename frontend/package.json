{
  "name": "HelloWorld",
  "private": true,
  "version": "0.0.0",
  "description": "Hello World",
  "main": "server.js",
  "author": {
    "name": "",
    "email": ""
  },
  "dependencies": {
    "express": "^4.17.3",
    "mocha": "^3.0.0"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-mocha": "^2.2.0",
    "mocha-junit-reporter": "^1.12.0",
    "gulp-istanbul": "^1.1.3",
    "nyc": "^12.0.2"
  },
  "scripts": {
    "tests": "nyc mocha --reporter=mocha-junit-reporter --reporter-options mochaFile=./TEST-RESULTS.xml tests/**/*.js"
  }
}
